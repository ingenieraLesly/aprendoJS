//Condicionales
/**
 * >mayor
 * <menor
 * >= (por detrás hay otro igual comparando)
 * <= (por detrás hay otro igual para comparar)
 * = asignación
 * == comparación de información o valor
 * === una comparación de valor y tipo de dato
*/
let edad = prompt("Ingresa tu edad");

if (edad >= 16) {
    alert("Tiene acceso a todo el catalogo");
} else {
    alert("El catalogo está bloquedao");
}
